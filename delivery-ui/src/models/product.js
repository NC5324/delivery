export default class Product {
  constructor (id, name, price, type, imgSource, toppings, newToppings) {
    this.id = id
    this.name = name
    this.price = price
    this.type = type
    this.imgSource = imgSource
    this.toppings = toppings
    this.newToppings = newToppings
  }
}
